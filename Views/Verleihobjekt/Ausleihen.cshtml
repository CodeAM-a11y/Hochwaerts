@model Hochwaerts.Models.Verleihobjekt

@{
    ViewData["Title"] = "Ausleihen";
}

<h1>Edit</h1>

<h4>Verleihobjekt</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Ausleihen">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ZaubererId" class="control-label"></label>
                <select asp-for="ZaubererId" class="form-control" asp-items="ViewBag.ZaubererId"></select>
                <span asp-validation-for="ZaubererId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}